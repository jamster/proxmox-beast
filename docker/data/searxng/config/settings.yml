general:
  debug: false
  instance_name: "SearXNG"

search:
  safe_search: 0
  autocomplete: ""

server:
  secret_key: "CHANGE_ME"  # Replace this with a random string
  limiter: false
  image_proxy: false

outgoing:
  request_timeout: 10.0  # Increased timeout
  max_request_timeout: 15.0  # Increased max timeout

engines:
  - name: soundcloud
    disabled: true  # Disable SoundCloud engine

redis:
  url: redis://redis:6379/0
